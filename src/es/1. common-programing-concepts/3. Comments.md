# Comentarios

```rust
// Line comment

/*
   Block comment — can span
   multiple lines
*/

/// Doc comment — generates docs (Markdown)
/// Use `cargo doc` to render.
fn documented_fn() {}

//! Module-level doc comment (at top of file)
```

> **Nota:** Usa `///` para documentación de API pública. Usa `//` para notas internas.
