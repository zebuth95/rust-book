# Advanced Features

Unsafe Rust, advanced traits, types, functions/closures, and macros.

**Topics:** Raw pointers, FFI, associated types, operator overloading, `!` (never type), `?Sized`, declarative & procedural macros.
