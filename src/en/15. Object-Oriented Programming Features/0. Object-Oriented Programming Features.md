# OOP Features in Rust

Rust isn't a traditional OOP language but supports OOP principles through:

| OOP Concept | Rust Mechanism |
|---|---|
| Encapsulation | `pub` / private fields |
| Polymorphism | Traits + Generics (static) / Trait Objects (dynamic) |
| Inheritance | **Not supported** — use composition & traits instead |
