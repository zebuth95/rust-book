# Advanced Features
Rust's safety guarantees and delves into powerful language features often used when building low-level abstractions, systems code, and domain-specific languages (DSLs). The core themes are bypassing safety checks when necessary (unsafe), maximizing trait expressiveness (associated types, specialization), and metaprogramming (macros).