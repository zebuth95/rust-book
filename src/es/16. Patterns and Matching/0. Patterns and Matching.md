# Patrones y Matching

Los patrones están en todas partes en Rust — `match`, `let`, `if let`, `for`, parámetros de función.

Los patrones pueden:

- Deconstruir structs, enums, tuplas, arrays.
- Coincidir con literales, rangos, comodines.
- Vincular variables (`@`) y añadir guardas (`if`).
