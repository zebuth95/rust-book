# Pruebas

Rust tiene soporte de primera clase para pruebas con `cargo test`.

- Las pruebas son funciones anotadas con `#[test]`.
- Se ejecutan con `cargo test`.
- Los archivos de prueba van en `src/` (unitarias) o `tests/` (integración).
