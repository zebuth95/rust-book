# Enums & Pattern Matching — Overview

Enums let you define a type by listing its possible values. `match` lets you handle each variant safely and exhaustively.

**Key types:** `Option<T>` (value or nothing), custom enums with data.

> **Note:** This chapter also introduces the `if let` syntax for concise single-pattern matching.
