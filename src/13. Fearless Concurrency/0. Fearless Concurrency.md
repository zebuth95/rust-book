# Fearless Concurrency

Rust's ownership system prevents data races at **compile time**.

## Concurrency models

- **Message passing** — channels (`std::sync::mpsc`).
- **Shared state** — `Arc<Mutex<T>>`.
- **`Send` & `Sync` traits** — built-in thread safety.
