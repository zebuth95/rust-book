# Object-Oriented Programming Features
Rust is not a traditional Object-Oriented Programming OOP language but can achieve OOP principles like polymorphism
and encapsulation using its **structs,traits and associated types**.
Rust avoids overhead and mandatory inheritance, favoring composition and explicit contracts defines by traits.
The core mechanism for dynamic polymorphism is the **Trait Object**.

