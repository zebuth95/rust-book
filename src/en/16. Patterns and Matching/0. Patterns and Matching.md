# Patterns & Matching

Patterns are everywhere in Rust — `match`, `let`, `if let`, `for`, function params.

Patterns can:

- Destruct structs, enums, tuples, arrays.
- Match literals, ranges, wildcards.
- Bind variables (`@`) and add guards (`if`).
