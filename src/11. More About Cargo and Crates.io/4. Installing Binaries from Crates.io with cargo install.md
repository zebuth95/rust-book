# `cargo install`

Install binary crates from crates.io to `~/.cargo/bin/`.

```bash
cargo install ripgrep     # rg
cargo install bat          # cat with syntax highlighting
cargo install fd-find      # fd
```

> **Note:** Only works for crates that produce binaries (have a `[[bin]]` or `src/main.rs`).
