# ¿Usar `panic!` o No Usar `panic!`?

| Usa `panic!` cuando... | Usa `Result` cuando... |
|---|---|
| Hay un bug o estado imposible | El fallo es esperado |
| Ejemplos, prototipos, pruebas | Código de librería |
| Sabes que el resultado es `Ok` | Quien llama debe decidir cómo manejar |

**Regla general:** Retorna `Result` desde funciones de librería. Deja que quien llama decida.

> **Nota:** Usa `.unwrap()` y `.expect()` con moderación — solo cuando estés seguro de que no fallarán.
