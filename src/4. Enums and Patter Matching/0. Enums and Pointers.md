# Enums and Patter Matching
We'll look at enumerations, also referred to us as enums.
Enums Allow ou to define a type by enumerating its possible variants.
There is a particular enum, called **Option**, which expresses that a value can be either something or nothing.

We'll look at how pattern matching in the **match** expression makes easy to sun code for different values and enum.

Finally, we''l cover how the **if** let construct is another convenient and concise idiom available to handle enums in your code.
